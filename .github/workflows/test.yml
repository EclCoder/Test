name: Decompile APK

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  decompile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache tools
        uses: actions/cache@v4
        with:
          path: $HOME/.cache/tools
          key: tools

      - name: Install tools
        run: |
          mkdir -p $HOME/.cache/tools/
          if [ ! -d $HOME/.cache/tools/jadx ]; then
            curl -L -o jadx.zip "https://github.com/skylot/jadx/releases/download/v1.5.6/jadx-1.5.6.zip"
            unzip jadx.zip -d $HOME/.cache/tools
            chmod +x $HOME/.cache/tools/jadx/bin/jadx
            echo "$HOME/.cache/tools/jadx/bin" >> "$GITHUB_PATH"
            rm jadx.zip
          fi

          if [ ! -d $HOME/.cache/tools/apktool ]; then
            mkdir -p $HOME/.cache/tools/apktool
            curl -L -o ~/.cache/tools/apktool/apktool.jar "https://github.com/iBotPeaches/Apktool/releases/download/v3.0.3/apktool_3.0.3.jar"
            cat > $HOME/.cache/tools/apktool/apktool <<'EOF'
exec java -jar "$(dirname "$0")/apktool.jar" "$@"
EOF
            chmod +x $HOME/.cache/tools/apktool/apktool
            echo "$HOME/.cache/tools/apktool/" >> "$GITHUB_PATH"
          fi

      - name: Install apk
        run: |
          
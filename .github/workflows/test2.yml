name: Test 2
on:
  push:
permissions:
  actions: read
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -L -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" -o file.zip "https://api.github.com/repos/EclCoder/Test/actions/artifacts/9899830069/zip"

          unzip file.zip
          ls
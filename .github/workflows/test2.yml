name: C Decompile
on:
  workflow_dispatch:
permissions:
  actions: read
  contents: write

jobs:
  decompile:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Cache tools
        uses: actions/cache@v4
        with:
          path: $HOME/.cache/ghidra
          key: ghidra-12.1.3

      - name: Install ghidra
        run: |
          
        
      - name: Test
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$HOME"
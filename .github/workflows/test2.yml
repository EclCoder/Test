name: Test 2
on:
  workflow_dispatch:
permissions:
  actions: read
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$HOME"